"""Shared library code for the road-network partition pipeline.

Pure, importable modules (no number prefixes, no top-level side effects beyond
``env_setup``). Run scripts under ``src/stages`` and ``src/*.py`` consume these.
"""

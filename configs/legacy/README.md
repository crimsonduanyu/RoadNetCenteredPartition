# Legacy configuration

`config.pre-refactor.yaml` is retained byte-for-byte for historical audit only.
It is not a runtime configuration and has no active code or test reader.

Use the split authoritative configurations under `configs/datasets/`,
`configs/zoning/` and `configs/pipelines/`.
